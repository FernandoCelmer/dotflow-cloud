[project]
name = "dotflow_cloud"
version = "0.1.0"
description = ""
requires-python = ">=3.9"
dependencies = [
    "dotflow (==1.0.0.dev2)",
    "httpx",
]

[project.optional-dependencies]
dev = ["pytest"]
